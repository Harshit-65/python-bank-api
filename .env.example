# Supabase Configuration (Required)
SUPABASE_URL=https://<your_project_ref>.supabase.co
SUPABASE_KEY=<your_supabase_service_role_key>

# Redis Configuration (Required)
# Example for local Redis:
# REDIS_URL=redis://localhost:6379
# Example for Redis Cloud (replace with your actual URL and password):
REDIS_URL=redis://<user>:<password>@hostname:port
REDIS_PASSWORD=<your_redis_password>

# Google AI API Keys (Required for parsing)
# Provide at least one key. Additional keys (GOOGLE_API_KEY2, etc.) can be added for load balancing.
GOOGLE_API_KEY=<your_google_ai_api_key>
# GOOGLE_API_KEY2=<another_google_ai_api_key>
# ... up to GOOGLE_API_KEY15

# Keyfolio Configuration (Required for Authentication)
KEYFOLIO_URL=<your_keyfolio_instance_url> # Or your Keyfolio instance URL
# KEYFOLIO_ADMIN_KEY=<your_keyfolio_admin_key> # Only needed for managing keys via Keyfolio admin tasks

# Optional: PKG_CONFIG_PATH (Usually only needed if encountering specific build issues on macOS/Linux)
# PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:/opt/homebrew/lib/pkgconfig:/opt/homebrew/opt/imagemagick/lib/pkgconfig
